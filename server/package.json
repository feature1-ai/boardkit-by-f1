{
  "name": "@feature1/boardkit-server",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hostable REST API server for the Boardkit engine — Postgres-backed (JSON-file fallback), with live updates over SSE.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.0",
    "pg": "^8.12.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/pg": "^8.11.0",
    "supertest": "^7.0.0",
    "vitest": "^2.0.0"
  }
}
